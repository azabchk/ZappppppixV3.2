# render.yaml
services:
  - type: web
    name: zappppppix-api
    runtime: docker              # use runtime for Docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeploy: true
    plan: free
    healthCheckPath: /health     # your FastAPI health endpoint

    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: zappppppix-db
          property: connectionString  # injects full Postgres URL
      - key: ADMIN_TOKEN
        generateValue: true
      - key: DEFAULT_INSTRUMENTS
        value: "RUB,USD"

services:
  - type: web
    name: zappppppix-api
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeploy: true
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        value: "postgresql://coding_with_azab_user:UMYpTCpTCPa0xkI07ZJuHgBvCPoTmFAC@dpg-d3vppd6r433s73dgeg10-a/coding_with_azab"
      - key: ADMIN_TOKEN
        generateValue: true
      - key: DEFAULT_INSTRUMENTS
        value: "RUB,USD"

# REMOVE the databases section completely